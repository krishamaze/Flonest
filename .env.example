# =============================================================================
# Cloud-Only Supabase Development Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# cp .env.example .env

# -----------------------------------------------------------------------------
# App Runtime (Frontend/Server)
# -----------------------------------------------------------------------------
# Get these from: Supabase Dashboard  Project Settings  API
VITE_SUPABASE_URL=https://yzrwkznkfisfpnwzbwfw.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# -----------------------------------------------------------------------------
# CLI Authentication (No Docker, No Service Key Required)
# -----------------------------------------------------------------------------
# Get this by running: npx supabase login
# The CLI uses ACCESS_TOKEN for authentication (not service role key)
SUPABASE_ACCESS_TOKEN=your-access-token-here

# One-time setup: Link to cloud project
# npm run supabase:link
# Or: npx supabase link --project-ref yzrwkznkfisfpnwzbwfw

# -----------------------------------------------------------------------------
# Database URLs (For Prisma/Drizzle/psql/Direct DB Access)
# -----------------------------------------------------------------------------
# Get these from: Supabase Dashboard  Project Settings  Database
# Use transaction pooler for best compatibility (supports IPv4 + IPv6)

# Transaction Pooler (Recommended - supports IPv4 + IPv6)
# Format: postgresql://postgres.[PROJECT_REF]:[PASSWORD]@[POOLER_HOST]:6543/postgres
DATABASE_URL=postgresql://postgres.yzrwkznkfisfpnwzbwfw:your-password@aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres

# Direct Connection (IPv6 only - may not work on all networks)
# Format: postgresql://postgres:[PASSWORD]@db.[PROJECT_REF].supabase.co:5432/postgres
DATABASE_DIRECT_URL=postgresql://postgres:your-password@db.yzrwkznkfisfpnwzbwfw.supabase.co:5432/postgres

# -----------------------------------------------------------------------------
# Optional: Local Development Overrides
# -----------------------------------------------------------------------------
# If you later set up local Supabase with Docker, you can override in .env.local
# Create .env.local (gitignored) for local overrides:
#
# VITE_SUPABASE_URL=http://127.0.0.1:54321
# VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
#
# Note: .env.local takes precedence over .env

# -----------------------------------------------------------------------------
# Setup Instructions
# -----------------------------------------------------------------------------
# 1. Copy this file: cp .env.example .env
# 2. Fill in VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY (from Supabase Dashboard)
# 3. Get ACCESS_TOKEN: npx supabase login
# 4. Fill in SUPABASE_ACCESS_TOKEN
# 5. Get DATABASE_URL from Supabase Dashboard  Database  Connection Pooling
# 6. Fill in DATABASE_URL and DATABASE_DIRECT_URL (optional)
# 7. Link project: npm run supabase:link
# 8. Start dev: npm run dev

# -----------------------------------------------------------------------------
# Security Notes
# -----------------------------------------------------------------------------
# - Never commit .env to git (already in .gitignore)
# - Never commit .env.local to git (already in .gitignore)
# - Rotate keys if they are ever exposed
# - Use different projects for dev/staging/production
# - ACCESS_TOKEN is for CLI only, ANON_KEY is for app runtime
